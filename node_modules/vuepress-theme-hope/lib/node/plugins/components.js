import { componentsPlugin } from "vuepress-plugin-components";
export const getComponentsPlugin = (options, { components = ["Badge", "FontIcon"], rootComponents = {}, } = {}, legacy = false) => componentsPlugin({
    // FontIcon component is used by theme so we MUST enable it
    components: components.includes("FontIcon")
        ? components
        : ["FontIcon", ...components],
    componentOptions: {
        fontIcon: {
            ...(options.iconAssets ? { assets: options.iconAssets } : {}),
        },
        ...(typeof options.iconPrefix === "string"
            ? { prefix: options.iconPrefix }
            : {}),
    },
    rootComponents: {
        ...rootComponents,
        backToTop: typeof options.backToTop === "number"
            ? options.backToTop
            : options.backToTop !== false,
    },
}, legacy);
//# sourceMappingURL=components.js.map