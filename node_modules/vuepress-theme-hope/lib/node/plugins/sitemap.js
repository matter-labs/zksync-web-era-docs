import { sitemapPlugin } from "vuepress-plugin-sitemap2";
export const getSitemapPlugin = (options, hostname, legacy = false) => {
    if (options === false)
        return null;
    const sitemapOptions = typeof options === "object" ? options : {};
    // disable sitemap if `hostname` is not set and no options for sitemap plugin
    if (!Object.keys(sitemapOptions).length && !hostname)
        return null;
    return sitemapPlugin({ hostname, ...sitemapOptions }, legacy);
};
//# sourceMappingURL=sitemap.js.map