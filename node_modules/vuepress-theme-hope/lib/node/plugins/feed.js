import { feedPlugin } from "vuepress-plugin-feed2";
import { deepMerge } from "vuepress-shared/node";
export const getFeedPlugin = (themeData, options, hostname, legacy = false) => {
    // disable feed if no options for feed plugin
    if (!Object.keys(options || {}).length)
        return null;
    return feedPlugin(
    // @ts-ignore
    deepMerge({
        hostname,
        ...(themeData.author ? { author: themeData.author } : {}),
        locales: Object.entries(themeData.locales).map(([localePath, { author, copyright }]) => [
            localePath,
            { author, channel: { copyright } },
        ]),
    }, options || {}, {
        customElements: [
            // @vuepress/plugin-external-link
            "ExternalLinkIcon",
            // vuepress-plugin-components
            "Badge",
            "BiliBili",
            "CodePen",
            "PDF",
            "StackBlitz",
            "VideoPlayer",
            "YouTube",
            // vuepress-plugin-md-enhance
            "ChartJS",
            "CodeDemo",
            "CodeTabs",
            "ECharts",
            "FlowChart",
            "Mermaid",
            "Playground",
            "Presentation",
            "Tabs",
            "VuePlayground",
            ...(options?.customElements || []),
        ],
    }), legacy);
};
//# sourceMappingURL=feed.js.map