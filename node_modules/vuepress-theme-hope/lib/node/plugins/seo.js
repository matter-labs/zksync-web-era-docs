import { seoPlugin } from "vuepress-plugin-seo2";
export const getSEOPlugin = (themeData, { seo }, hostname = "", legacy = false) => {
    if (seo === false)
        return null;
    const seoOptions = typeof seo === "object" ? seo : {};
    // disable seo if `hostname` is not set and no options for seo plugin
    if (!Object.keys(seoOptions).length && !hostname)
        return null;
    return seoPlugin({
        hostname,
        ...(themeData.author ? { author: themeData.author } : {}),
        isArticle: ({ filePathRelative, frontmatter }) => Boolean(filePathRelative) &&
            !frontmatter["home"] &&
            frontmatter["article"] !== false,
        ...seoOptions,
    }, legacy);
};
//# sourceMappingURL=seo.js.map