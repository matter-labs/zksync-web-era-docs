import type { Page } from "@vuepress/core";
import type { SidebarInfo, SidebarSorterFunction } from "../../../shared/index.js";
export interface FileInfo {
    type: "file";
    filename: string;
    path: string;
}
export interface DirInfo {
    type: "dir";
    dirname: string;
    path: string;
    items: (DirInfo | FileInfo)[];
}
export interface ThemeSidebarInfoOptions {
    pages: Page[];
    sorters: SidebarSorterFunction[];
    scope: string;
}
export declare const getSidebarInfo: ({ pages, sorters, scope, }: ThemeSidebarInfoOptions) => SidebarInfo[];
