import { KatexOptions } from 'katex';
import { PluginWithOptions } from 'markdown-it';

interface MarkdownItKatexOptions extends KatexOptions {
    /**
     * Whether enable mhchem extension
     *
     * 是否启用 mhchem 扩展
     *
     * @default false
     */
    mhchem?: boolean;
}

declare const katex: PluginWithOptions<MarkdownItKatexOptions>;

export { MarkdownItKatexOptions, katex };
